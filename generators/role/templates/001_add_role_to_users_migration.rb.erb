class <%= migration_name %> < ActiveRecord::Migration
  def self.up
    add_column "<%= users_table_name %>", :role, :string, :limit => 20
    add_index "<%= users_table_name %>", :role
  end

  def self.down
    remove_column "<%= users_table_name %>", :role
  end
end